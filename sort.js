const numbers = [3, 6, 2, 8, 1];
const strings = [
    'timeoutsRemaining',
    'flagrantFouls',
    'defensive3Seconds',
    'jumpshots',
    'dunks',
    'layups',
];

const sortedNumbers = numbers.sort();//정렬
const sortedStrings = strings.sort();//정렬

console.log(sortedNumbers);//정렬된 sortesNumbers
console.log(sortedNumbers.reverse());//뒤집어서
console.log(sortedStrings);//정렬된 sortedStrings
console.log(sortedStrings.reverse());//뒤집어서